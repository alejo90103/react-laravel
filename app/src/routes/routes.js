export const login = () => {
  return '/login';
}

export const logout = () => {
  return '/logout';
}

export const register = () => {
  return '/register';
}

export const home = () => {
  return '/home';
}